import { HideByClassDirective } from './hide-by-class.directive';

describe('HideByClassDirective', () => {
  it('should create an instance', () => {
    const directive = new HideByClassDirective();
    expect(directive).toBeTruthy();
  });
});
